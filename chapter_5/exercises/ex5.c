#include <stdio.h>

int main(void) {
    int i,j,k,n;
    i = 3;
    j = 2;
    n = 0;

    if (n >= 1 <= 10)
        printf("n is between 1 and 10\n");
    // it should still run the function even when n is = to 0
    
}
