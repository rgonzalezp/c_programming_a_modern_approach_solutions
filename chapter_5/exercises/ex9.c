#include <stdio.h>

int main(void) {
    
    // Too lazy to write out everything but
    // They are not equivalent. Depending on the bell curve of grades, 
    // one solution will be more efficient than the other one.
    // If more students get good scores ( 80 + ), the 1st program on average will have to do less checks
    // The opposite case applies to the 2nd program.
    
}
