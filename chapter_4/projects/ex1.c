#include <stdio.h>

int main(void) {

    int fdigit,sdigit;
    printf("Enter a two-digit number: ");
    scanf("%1d%1d",&fdigit,&sdigit);
    printf("The reversal is: %d%d\n", sdigit,fdigit);
    
    
    return 0;
}
